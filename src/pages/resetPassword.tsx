import ResetPassword from "../components/dashboard/changePassword/ChangePasswordForm"; // Adjust the path

const ResetPasswordPage = () => {
  return <ResetPassword />;
};

export default ResetPasswordPage;
